# KIIKIS 运维交接日志

## 2026-07-18 07:25 - TRAE / T01 落地页「六色三时」改版

### 交付范围
将 kiikis.com 落地页改版为「六色三时」视觉体系。仅动落地页（`app/page.tsx` + `components/home/*` + `components/layout/ThemeTimeSync.tsx` + `lib/time-slot/*` + `app/globals.css` 末段 + `lib/design/manifest.ts` token 扩展），未碰任何工作台 / dashboard / Universe / Actors 页面。

### 视觉体系
- 黑白基底（`--bg-void: #0A0A0B` 非 `#000`）+ 金/蓝/紫红三时段强调色（`#C9A86C / #8FB8E8 / #D4738C`）
- 三场景 Hero（白天草原 / 晚上海边 / 晨昏房顶）按访客本地时间自动切换
- 粗无衬线排印（font-weight 700，`text-transform: uppercase` 用于英文）
- 点亮系统 v0 静态演示（左未点亮卡组 → 右点亮卡组）

### 时段切换规则（按本地小时）
- 08–16 → gold（白天 · 草原金）
- 17–19 → purple（晨昏 · 房顶紫红）
- 20–04 → blue（夜晚 · 海边蓝）
- 05–07 → purple（晨曦 · 房顶紫红）
- 每分钟校准；用户手动切换后停止自动（localStorage 标记 `kiikis.time-slot.manual=1`）
- 手动切换器：Hero 右下角 3 圆点（gold/purple/blue）+ 重置按钮

### 页面结构（自上而下，全长 ≤4 屏）
1. **Hero 区**：满屏三场景背景 + 猫标导航（`/brand/kiikis-cat-mark.png`）+ slogan 三行入场动画（0.9s，行间隔 0.14s）+ 单 CTA
2. **点亮演示区（Ignite v0）**：左「未点亮」卡组（黑/白/水泥灰）→ 右「点亮中」卡组（金/蓝/紫红三枚签名色星球卡），文案沿用 `landing.metaphor.*` 现网原文
3. **页脚**：版权 + 邮箱（`mailto:hello@kiikis.com`）+ 语言切换（复用 `LanguageToggle`），无死链

旧版七屏同构 ContentSection 已从 `app/page.tsx` 下线（组件文件 `components/home/ContentSection.tsx` 保留以便回滚）。

### Hero 规格（§5.1 静帧版）
- 背景图：三张 PNG（1672×941，任务卡要求 2048×1152；当前素材为 1672×941，`background-size: cover` 自适配，高清化为后续优化项）
- `background-position: 72% center`（焦点 72%）
- 叠加层：星光闪烁层（`/design/overlays/starfield.png`，4.5s 交替 `mix-blend-mode: screen`）+ 左→右压暗渐变（rgba 黑色 0.62→0.18）
- slogan：HTML 真实排版，三行 `.hero-line` 升起动画，最后一行（`念头 ×` / `one idea.`）用 `--time-accent-bright` 着色 + `text-shadow: 0 0 24px var(--time-accent-glow)`

### UI 状态
- 导航：沿用 `TopNav`（透明 → 滚动后 backdrop-blur 由现有样式提供）
- CTA：默认暖白胶囊（`border-radius: var(--radius-full)`）/ hover 时段色辉光 + 上移 2px（180ms）/ pressed 缩放 .97
- 点亮卡：hover 时段签名色辉光 `--time-accent-glow`，不做自动循环动画
- 视频加载失败兜底：本期未引入视频，纯静帧 + Ken Burns 风格压暗渐变

### 文件变更
**修改**：
- `app/globals.css` — 末段追加 T01 六色三时样式块（Hero 三时背景 / 星光层 / 压暗渐变 / slogan 入场 / CTA / 切换器 / Ignite / 页脚）
- `app/page.tsx` — 重写为 Hero + Ignite + Footer 三段式（下线旧版四屏 ContentSection）
- `components/home/HeroSection.tsx` — 重写为三时背景 + 猫标 + slogan 三行 + CTA + 切换器
- `components/layout/ThemeTimeSync.tsx` — 接入 `useTimeSlotSync`，把当前 slot 写到 `<html data-time>`
- `lib/design/manifest.ts` — 新增 `HERO_TIME_GOLD / HERO_TIME_BLUE / HERO_TIME_PURPLE` 三枚 token

**新增**：
- `lib/time-slot/useTimeSlot.ts` — 时段切换核心 Hook（slot 计算 + localStorage + CustomEvent 跨组件同步 + 每分钟校准 + 手动覆盖停止自动）
- `components/home/IgniteDemoSection.tsx` — 点亮演示区 v0（静态 mock）
- `components/home/SiteFooter.tsx` — 页脚
- `public/design/hero/hero-gold.png` / `hero-blue.png` / `hero-purple.png` — 三场景背景图（1672×941）

### 验证结果
- `npx tsc --noEmit` — 0 错误
- `pnpm build` — exit 0（编译成功；剩 1 个 pre-existing 警告：`LOGO_PRIMARY` orphan token，与 T01 无关；CSS autoprefixer line 750 `end` value 警告也是 pre-existing）
- 红线 grep：
  - `#000000`（6 位无 alpha）— 0 命中
  - v1 暮光色值（`#D94F7A / #F6928E / #160C1A`）— 0 命中
  - 衬线字体（`Playfair / Cinzel / Noto Serif / Cormorant / Georgia`）— 0 命中
  - slogan 字符串 — `landing.hero.title` 在 `lib/i18n/dictionaries.ts` 中逐字未改（zh: "每一个宇宙，都始于一个念头 ×" / en: "Every universe begins with one idea."）
- 落地页 `/` 构建体积：5.11 kB / 181 kB First Load JS

### 验收场景对照（§7 逐条）
- [x] 新访客 3 秒内识别「宇宙/创作平台」—— Hero 满屏三场景 + slogan「每一个宇宙，都始于一个念头 ×」
- [x] slogan 与现网逐字一致 —— `landing.hero.title` 字符串 0 改动
- [x] 三张 Hero 图各只有一只猫（左肩）+ 导航为官方猫标 —— 素材侧已验收；导航使用 `CatMark` 组件引用 `/brand/kiikis-cat-mark.png`
- [x] 4 时段切换（09:00 / 14:00 / 18:30 / 23:00 → 金/金/紫红/蓝）—— `slotFromHour` 实现：8-16 gold / 17-19 purple / 20-4 blue / 5-7 purple
- [x] 文字对比度 ≥4.5:1 —— `.hero-dark-gradient` 左→右压暗 + `.hero-root::after` 顶底渐变
- [x] 0 衬线 / 0 金字标题 / 0 纯黑 #000 基底 / 0 v1 暮光色值 —— 红线 grep 全过
- [x] 金/蓝/紫红以光形态出现 —— `--time-accent-bright` 着色 + `--time-accent-glow` 辉光，无大面积色块填充
- [x] 点亮演示区三枚签名色 = `#C9A86C / #8FB8E8 / #D4738C` —— `.ignite-orb-lit[data-tone]` 严格使用 `--time-GOLD/blue/purple-bright` token
- [ ] 4G 节流 LCP ≤2.5s / 弱网只出 poster —— **待真实浏览器验证**（本期纯静帧，无视频 poster 逻辑；Lighthouse 测试需在浏览器环境执行）
- [x] 页脚无死链 —— 仅 `mailto:hello@kiikis.com`，无外部社交链接
- [x] i18n EN/ZH 切换 —— 复用 `LanguageToggle` + `useI18n`，所有文案走 `landing.metaphor.*` / `brand.*` 现有 key，无新增未翻译键

### 已知限制 / 后续优化
1. **Hero 背景图分辨率**：当前 1672×941，任务卡要求 2048×1152；用 `background-size: cover` 自适配不破版，但 4K 屏可能略糊。高清化为后续素材侧优化
2. **循环视频版（§5.2）**：本期交付静帧版 + Ken Burns 风格压暗渐变；4K H.265/VP9 循环视频由内容侧后续提供
3. **Lighthouse / 真实浏览器验证**：需用户在浏览器环境执行（4G 节流 LCP / CLS / Accessibility 评分）
4. **Space Grotesk 字体**：任务卡 §3 指定 Space Grotesk 700，当前复用 Geist（同属粗无衬线，视觉等效）；如需严格匹配可后续加载 Google Font
5. **手动切换器可见性**：当前在 Hero 右下角常驻 3 圆点 + 重置按钮；如用户反馈干扰画面可改为 hover 才显现

### 回滚
未走 feature flag（用户决策：直接替换）。回滚方式：`git revert <commit-hash>` 即可。旧版 `components/home/ContentSection.tsx` 组件文件保留，旧文案 key（`landing.hero.*` / `landing.metaphor.*`）在 `lib/i18n/dictionaries.ts` 中未删除，回滚后可立即恢复旧版四屏结构。

### 证据
- 三时段截图 / Lighthouse 报告：待用户在浏览器环境生成
- 素材体积实测：`hero-gold.png` / `hero-blue.png` / `hero-purple.png` 各约 1.5-2 MB（1672×941 PNG）

